# deps cache and bundles
node_modules
.parcel-cache
build

# keep directories with .gitkeep files in them checked in, but not their contents
__data/*/*
!__data/*/.gitkeep

# every service has their own env file
*.env

# create and run these during development, instead of running an entire service, don't check them in tho!
*.sandbox.js

# junk
.DS_Store

# TODO: this should be generated during an ARM build, not on a dev machine, not committing
yarn.lock
